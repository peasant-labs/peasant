## peasant prune

Remove sessions from local store and database

### Synopsis

Permanently delete sessions matching the given filters from the local SQLite database and filesystem.

```
peasant prune [flags]
```

### Options

```
      --after string      Prune sessions started after this date (YYYY-MM-DD)
      --all               Prune all sessions
      --before string     Prune sessions started before this date (YYYY-MM-DD)
      --confirm           Skip interactive confirmation (for scripts/CI)
      --dry-run           Show what would be deleted without deleting
      --harness string    Prune sessions by harness (claude-code, gemini-cli, codex, opencode)
  -h, --help              help for prune
      --json              Output results as JSON
      --project string    Prune sessions by project hash
      --session strings   Session IDs to prune (repeatable)
      --unselected        Prune sessions not selected by harness, project, clone, branch, or explicit session rules (requires selection.mode=selected)
```

### Options inherited from parent commands

```
      --config string       Path to config file (default "~/.config/peasant/config.yaml")
      --config-dir string   Override the config directory (default: $XDG_CONFIG_HOME or ~/.config); config lives under <config-dir>/peasant
      --data-dir string     Override the data directory (default: $XDG_DATA_HOME or ~/.local/share); the DB + peasant-sync live under <data-dir>/peasant
      --state-dir string    Override the state directory (default: $XDG_STATE_HOME or ~/.local/state); logs/PID live under <state-dir>/peasant
```

### SEE ALSO

* [peasant](peasant.md)	 - Developer observability for AI coding agent sessions

###### Auto generated by spf13/cobra on 5-Sep-2026
