## peasant annotate sample

Sample random sessions for annotation

```
peasant annotate sample [flags]
```

### Options

```
  -n, --count int                  Number of sessions to sample (default 15)
  -h, --help                       help for sample
      --max-total-turns int        Maximum total indexed turns (all roles, all depths) (default 2000)
      --min-user-turns int         Minimum depth-0 user turns (from indexed session_entries, not raw transcript) (default 3)
      --only-parent-sessions       Only sample parent sessions (exclude subagents/teammates) (default true)
      --project string             Filter to a specific project (substring match)
      --seed int                   Random seed for reproducible sampling (0 = random)
      --session-from-file string   File of existing session IDs (one per line) to include and exclude from new sampling
      --unannotated                Only sessions without friction episode annotations
```

### Options inherited from parent commands

```
      --config string       Path to config file (default "~/.config/peasant/config.yaml")
      --config-dir string   Override the config directory (default: $XDG_CONFIG_HOME or ~/.config); config lives under <config-dir>/peasant
      --data-dir string     Override the data directory (default: $XDG_DATA_HOME or ~/.local/share); the DB + peasant-sync live under <data-dir>/peasant
      --state-dir string    Override the state directory (default: $XDG_STATE_HOME or ~/.local/state); logs/PID live under <state-dir>/peasant
```

### SEE ALSO

* [peasant annotate](peasant_annotate.md)	 - Manage session and project annotations

###### Auto generated by spf13/cobra on 8-Aug-2026
