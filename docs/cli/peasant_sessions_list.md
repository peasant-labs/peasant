## peasant sessions list

List sessions

### Synopsis

List ingested sessions with optional filtering. Outputs a table by default or JSON with --json.

```
peasant sessions list [flags]
```

### Options

```
      --harness string   Filter by harness (claude-code, gemini-cli, codex, opencode)
  -h, --help             help for list
      --json             Output as JSON array
      --limit int        Maximum number of sessions to show (0 = no limit) (default 20)
      --project string   Filter by project name (matches git remote URL or directory basename)
      --reverse          Reverse sort order (ascending instead of descending)
      --since string     Show sessions starting after this date (e.g. 7d, 24h, 2026-01-01)
      --sort string      Sort by field (date, turns, tokens, project) (default "date")
      --tag string       Filter by session tag
      --until string     Show sessions starting before this date (e.g. 7d, 24h, 2026-01-01)
```

### Options inherited from parent commands

```
      --config string       Path to config file (default "~/.config/peasant/config.yaml")
      --config-dir string   Override the config directory (default: $XDG_CONFIG_HOME or ~/.config); config lives under <config-dir>/peasant
      --data-dir string     Override the data directory (default: $XDG_DATA_HOME or ~/.local/share); the DB + peasant-sync live under <data-dir>/peasant
      --state-dir string    Override the state directory (default: $XDG_STATE_HOME or ~/.local/state); logs/PID live under <state-dir>/peasant
```

### SEE ALSO

* [peasant sessions](peasant_sessions.md)	 - Manage sessions

###### Auto generated by spf13/cobra on 29-Jul-2026
