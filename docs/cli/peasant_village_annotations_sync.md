## peasant village annotations sync

Refresh foreign annotations on your pushed transcripts (login required)

### Synopsis

Refresh the foreign annotations (authored by other village users) on the
transcripts you have pushed. Your own authored annotations are excluded. Pulled
annotation data is foreign and one-way: it never re-enters your annotate-push
candidate set.

Without --session, all your pushed transcripts are scanned. With --session
<local-id>, only the single pushed transcript correlated to that local session
ID is refreshed.

```
peasant village annotations sync [flags]
```

### Options

```
  -h, --help             help for sync
      --json             Output as JSON instead of human-readable
      --quiet            Suppress the summary; print only errors and a final result line
      --session string   Refresh only the pushed transcript correlated to this local session ID (default: all)
      --verbose          Show extra detail (village host, transcripts scanned, excluded own-authored count)
```

### Options inherited from parent commands

```
      --config string       Path to config file (default "~/.config/peasant/config.yaml")
      --config-dir string   Override the config directory (default: $XDG_CONFIG_HOME or ~/.config); config lives under <config-dir>/peasant
      --data-dir string     Override the data directory (default: $XDG_DATA_HOME or ~/.local/share); the DB + peasant-sync live under <data-dir>/peasant
      --state-dir string    Override the state directory (default: $XDG_STATE_HOME or ~/.local/state); logs/PID live under <state-dir>/peasant
```

### SEE ALSO

* [peasant village annotations](peasant_village_annotations.md)	 - Sync foreign annotations on your pushed transcripts

###### Auto generated by spf13/cobra on 8-Aug-2026
