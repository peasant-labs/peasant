## peasant upgrade

Upgrade Peasant

### Synopsis

Upgrade Peasant from GitHub release artifacts. Raw archive installs are replaced in place after verifying checksums.txt. Package-manager-owned installs are not modified; the command prints the manager-owned upgrade path so package metadata stays correct. Stable builds use the latest stable release by default; prerelease builds look for prerelease updates too. Use --prerelease to opt in from a stable build, or --version for an exact release. Use --allow-downgrade with --version only for an intentional rollback to an older release. Use --yes only when automation has already reviewed and accepted the printed replacement plan.

```
peasant upgrade [flags]
```

### Options

```
      --allow-downgrade   Allow installing an older release when paired with --version
      --dry-run           Print the upgrade plan without changing files
  -h, --help              help for upgrade
      --prerelease        Allow the newest prerelease when --version is not set
      --version string    Install a specific release tag or version
      --yes               Accept the raw archive replacement plan without an interactive prompt
```

### Options inherited from parent commands

```
      --config string       Path to config file (default "~/.config/peasant/config.yaml")
      --config-dir string   Override the config directory (default: $XDG_CONFIG_HOME or ~/.config); config lives under <config-dir>/peasant
      --data-dir string     Override the data directory (default: $XDG_DATA_HOME or ~/.local/share); the DB + peasant-sync live under <data-dir>/peasant
      --state-dir string    Override the state directory (default: $XDG_STATE_HOME or ~/.local/state); logs/PID live under <state-dir>/peasant
```

### SEE ALSO

* [peasant](peasant.md)	 - Developer observability for AI coding agent sessions

###### Auto generated by spf13/cobra on 5-Sep-2026
