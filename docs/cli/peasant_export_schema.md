## peasant export schema

Export annotation types as structured schema configs

### Synopsis

Export Peasant annotation types to a structured schema format.

Use --shape to select the output format:
  eee  (default) — EveryEvalEver metric configs

Maps Peasant annotation types to EEE metric types:
  - enumerated + text        -> categorical (nominal or ordinal)
  - described  + real/integer -> numeric (continuous)
  - described/enumerated + boolean -> boolean
  - described  + text        -> categorical (nominal)

Output includes configs array and summary statistics.

```
peasant export schema [flags]
```

### Options

```
      --all             Include deprecated and retired types
  -h, --help            help for schema
      --json            Output as JSON instead of human-readable
      --shape string    Schema shape to export (currently only 'eee') (default "eee")
      --status string   Filter by annotation type status (active, proposed, deprecated, retired)
```

### Options inherited from parent commands

```
      --config string       Path to config file (default "~/.config/peasant/config.yaml")
      --config-dir string   Override the config directory (default: $XDG_CONFIG_HOME or ~/.config); config lives under <config-dir>/peasant
      --data-dir string     Override the data directory (default: $XDG_DATA_HOME or ~/.local/share); the DB + peasant-sync live under <data-dir>/peasant
      --state-dir string    Override the state directory (default: $XDG_STATE_HOME or ~/.local/state); logs/PID live under <state-dir>/peasant
```

### SEE ALSO

* [peasant export](peasant_export.md)	 - Export session data, annotations, and schemas

###### Auto generated by spf13/cobra on 11-Aug-2026
