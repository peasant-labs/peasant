# Exact case set for the offline capability scan and fresh receiver negotiation
# fixture. Name-only: no bare count, so adding a case never edits this file, and
# removing or renaming one fails until the name is dropped here.
requiredNames:
  - offline_scan_derives_graph_requirement_with_no_network
  - offline_scan_plain_payload_requires_nothing
  - fresh_supported_negotiation_uploads_graph_payload
  - store_without_generation_support_publishes_legacy_envelope
  - support_changed_after_scan_to_unsupported_refuses
  - omitted_advertisement_refuses_graph_payload
  - future_revision_token_does_not_satisfy_v1
  - unknown_token_alone_does_not_satisfy_v1
  - unknown_extra_token_with_v1_uploads
  - unavailable_preflight_refuses_graph_payload
  - unavailable_preflight_keeps_plain_payload_compatible
  - plain_payload_with_empty_advertisement_uploads
  - null_advertisement_refuses_graph_payload
  - count_only_requires_graph_token
  - root_only_requires_graph_token
  - purpose_only_requires_graph_token
  - provenance_only_requires_graph_token
  - source_ref_only_requires_no_token
  - count_only_supported_has_no_shortfall
