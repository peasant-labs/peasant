# The production kickstart mount seeds transient retention before constructing
# Flow. The count guard makes loss of this mounted call-site proof explicit.
expectedRetentionSeedRows: 1
retentionSeeds:
  - name: existing claude cleanup value starts clean
    cleanupDays: 90
    selectedOption: "(•) 90 days"
    cleanReceipt: no changes to save
