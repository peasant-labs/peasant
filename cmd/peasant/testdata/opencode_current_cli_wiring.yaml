# required_cases names the cases that must stay present; removing one goes red
# by name. Adding a case need not appear here.
required_cases:
  - current-sqlite-is-ingested-through-normal-cli-construction
cases:
  - name: current-sqlite-is-ingested-through-normal-cli-construction
    source_fixture: semantic-parity-current
    expected_sessions: 1
